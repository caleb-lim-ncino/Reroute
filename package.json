{
  "name": "reroute",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --use-system-ca run --watch src/index.ts",
    "start": "bun --use-system-ca run src/index.ts",
    "probe:tfl": "bun --use-system-ca run scripts/probe-tfl.ts",
    "probe:agent": "bun --use-system-ca run scripts/probe-agent.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.280",
    "hono": "^4.6.14",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
